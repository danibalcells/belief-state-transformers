[project]
name = "belief-state-transformers"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ipython>=9.10.0",
    "matplotlib>=3.10.8",
    "torch>=2.10.0",
    "transformer-lens>=2.17.0",
]
